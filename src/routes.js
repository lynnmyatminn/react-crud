import Dashboard from "./components/Dashboard/Dashboard";

const routes = [
  {
    path: "/dashboard",
    name: "Dashboard",
    component: Dashboard,
    exact: true,
  },
];

export default routes;
